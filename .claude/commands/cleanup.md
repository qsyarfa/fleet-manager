Clean up the current codebase before committing:
1. Remove all console.log statements
2. Remove unused imports
3. Ensure all fleet-* tokens are used correctly
4. Check all new components follow CLAUDE.md conventions
Report what was cleaned, commit nothing automatically.
